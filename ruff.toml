line-length = 120

[lint]
select = ["E", "F", "W", "I"]
ignore = [
  "E501",  # line too long — handled by formatter preference
]

[lint.isort]
known-first-party = ["config", "sync", "sheets", "categorize"]

[lint.per-file-ignores]
"sync_job.py" = ["F401"]
