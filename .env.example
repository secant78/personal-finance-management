# Only needed to configure AWS CLI authentication.
# On EC2/Lambda/ECS, IAM roles handle this automatically — no file needed.
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key
AWS_DEFAULT_REGION=us-east-1
